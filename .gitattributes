# Normalize line endings so Windows checkouts stop producing whole-file diffs.
# Git stores LF; working copies get native endings.
* text=auto

# Binary assets: never touch.
*.png binary
*.jpg binary
*.wav binary
*.mp3 binary
