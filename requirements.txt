# Pinned so an install months from now behaves like one today.
# Resolved 2026-08-02. To refresh: pip install -U <pkg>, then pip freeze to check.
#
# pygame vs pygame-ce: these provide the SAME `pygame` module and clobber each
# other. Only pygame-ce is listed here. Do not add plain `pygame` back.
#
# twitchio is held below 3.0 deliberately: the code uses the 2.x API
# (commands.Bot(token=..., initial_channels=...)). 3.x will not start.
#
# simple-websocket isn't imported anywhere, but Flask-SocketIO needs it to serve
# real websockets in threading mode. Without it it logs "WebSocket transport not
# available" and falls back to HTTP long-polling, which still works but adds
# latency to every overlay update.

azure-cognitiveservices-speech==1.51.1
Flask==3.1.3
Flask-SocketIO==5.6.1
gTTS==2.5.4
mutagen==1.48.1
obs-websocket-py==1.0
pydub==0.25.1
pygame-ce==2.5.7
pytz==2026.3.post1
simple-websocket==1.1.0
soundfile==0.14.0
twitchio==2.10.0
